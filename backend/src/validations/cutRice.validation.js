const yup = require('yup');

const create = yup.object({
  userId: yup.string().uuid('Ma nguoi dung khong hop le').required('Truong nay la bat buoc'),
  semesterId: yup.string().uuid('Ma hoc ky khong hop le').nullable(),
  weekStartDate: yup.date().nullable(),
  weekEndDate: yup.date().nullable(),
  weekly: yup.object().nullable(),
  isAutoGenerated: yup.boolean().nullable(),
  lastUpdated: yup.date().nullable(),
  notes: yup.string().max(255).nullable(),
});

const update = yup.object({
  userId: yup.string().uuid('Ma nguoi dung khong hop le').nullable(),
  semesterId: yup.string().uuid('Ma hoc ky khong hop le').nullable(),
  weekStartDate: yup.date().nullable(),
  weekEndDate: yup.date().nullable(),
  weekly: yup.object().nullable(),
  isAutoGenerated: yup.boolean().nullable(),
  lastUpdated: yup.date().nullable(),
  notes: yup.string().max(255).nullable(),
});

const request = yup.object({
  semesterId: yup.string().uuid('Ma hoc ky khong hop le').required('Hoc ky la bat buoc'),
  weekStartDate: yup.date().required('Tuan ap dung la bat buoc'),
  weekly: yup.object().required('Lich cat com la bat buoc'),
  notes: yup.string().max(255).nullable(),
});

const review = yup.object({
  reviewNote: yup.string().max(255).nullable(),
});

module.exports = { create, update, request, review };
