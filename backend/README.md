# WEB DA
# backend

## Database

Chay lenh sau de cap nhat schema database va seed lai du lieu mau:

```bash
npm run db:refresh
```

Lenh nay su dung `sequelize.sync({ force: true })`, vi vay database hien tai se bi reset truoc khi nap du lieu moi.
