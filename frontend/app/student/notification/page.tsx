import Main from "@/components/student/notification/Main";

export const metadata = {
  title: "Thông báo | Cổng học viên",
  description:
    "Xem danh sách thông báo, lọc theo loại và trạng thái đọc của học viên.",
};

export default function StudentNotificationPage() {
  return <Main />;
}
