import Main from "@/components/commander/notification/Main";

export const metadata = {
  title: "Thông báo | Cổng chỉ huy",
  description:
    "Xem danh sách thông báo, lọc theo loại và trạng thái đọc của chỉ huy.",
};

export default function CommanderNotificationPage() {
  return <Main />;
}
